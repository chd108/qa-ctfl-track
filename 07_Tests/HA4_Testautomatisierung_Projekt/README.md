# HA4 — Projekt Testautomatisierung (GroceryMate)

Platzhalter für die **Umsetzung** laut `Aufgabenstellung_HA4_Testautomatisierung_Projekt.md`.

- **Tests:** `tests/`
- **Page Objects:** `pages/`
- **Konstanten / Hilfen:** `utils/`
- **Artefakte:** `screenshots/` (bei Fehlern), `reports/` (z. B. HTML)

PyCharm-Projekt lokal an diesen Ordner binden oder Inhalt hier entwickeln und versionieren.
